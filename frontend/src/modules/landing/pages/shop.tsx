import { type FC } from 'react';

import { ShopView } from '@/common/components/shop/shop-view';

/** Visitor-facing shop. Product links resolve against the site root. */
const LShop: FC = () => <ShopView />;

export default LShop;
